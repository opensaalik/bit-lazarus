export default function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="eyebrow">Bit Lazarus</p>
        <h1>Frontend scaffold ready.</h1>
        <p>
          The React and Vite app is wired to live under <code>/app</code>. The next
          change adds the Three.js interface, wallet login surface, and bounty views.
        </p>
      </section>
    </main>
  );
}

